---
phase: 01-functional-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/tsconfig.json
  - dashboard/backend/server.ts
  - dashboard/backend/routes/index.ts
  - dashboard/backend/adapters/session.ts
  - dashboard/backend/adapters/config.ts
  - tests/dashboard-backend/bootstrap.test.ts
autonomous: true
requirements:
  - MIG-01
user_setup: []
must_haves:
  truths:
    - A dedicated Node backend entry exists for the new dashboard scope without replacing the legacy runtime.
    - The backend can boot in the current Node.js 22 ESM + PM2 environment.
  artifacts:
    - dashboard/tsconfig.json
    - dashboard/backend/server.ts
    - dashboard/backend/routes/index.ts
    - tests/dashboard-backend/bootstrap.test.ts
  key_links:
    - The backend entry composes route registration through a small index layer.
    - Session/config adapters are isolated from route composition.
---

<objective>
Establish the brownfield-safe backend bootstrap for the new dashboard scope.

Purpose: Create a dedicated backend surface that can coexist with the existing HYDRA runtime and give later plans a stable execution target.
Output: A focused TypeScript config, backend server entry, route registration skeleton, session/config adapters, and bootstrap coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-functional-backend-foundation/01-RESEARCH.md
@scripts/dashboard.js
@ecosystem.config.cjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the dedicated dashboard backend runtime</name>
  <files>dashboard/tsconfig.json, dashboard/backend/server.ts, dashboard/backend/routes/index.ts</files>
  <action>Create the minimal backend runtime for the new dashboard scope using TypeScript and ESM-compatible conventions that fit the current HYDRA repo. Keep the server bootstrap small and declarative. Do not delete or rewrite the legacy `scripts/dashboard.js`; this plan is about coexistence, not replacement.</action>
  <verify>npx tsc --noEmit -p dashboard/tsconfig.json</verify>
  <done>A compilable backend entry exists and route registration is composed through a dedicated index layer.</done>
</task>

<task type="auto">
  <name>Task 2: Isolate auth/config concerns into backend adapters</name>
  <files>dashboard/backend/adapters/session.ts, dashboard/backend/adapters/config.ts, dashboard/backend/server.ts</files>
  <action>Move session/config access behind small backend adapter functions so later route plans do not read env vars or auth state inline. Keep the adapter API functional and side-effect boundaries obvious.</action>
  <verify>npx tsc --noEmit -p dashboard/tsconfig.json</verify>
  <done>Server bootstrap consumes session/config adapters instead of mixing env/auth concerns into route wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Add bootstrap coverage for the new backend entry</name>
  <files>tests/dashboard-backend/bootstrap.test.ts, dashboard/backend/server.ts</files>
  <action>Add a focused bootstrap test that proves the backend entry and route index compose without colliding with the existing HYDRA runtime assumptions. Keep the test targeted at backend startup and registration behavior.</action>
  <verify>npx vitest run tests/dashboard-backend/bootstrap.test.ts</verify>
  <done>The backend bootstrap has automated coverage and can be executed in isolation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit -p dashboard/tsconfig.json`
- [ ] `npx vitest run tests/dashboard-backend/bootstrap.test.ts`
- [ ] Backend bootstrap coexists with `scripts/dashboard.js` without replacing it
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dedicated backend bootstrap exists and is brownfield-safe
</success_criteria>

<output>
After completion, create `.planning/phases/01-functional-backend-foundation/01-01-SUMMARY.md`
</output>
