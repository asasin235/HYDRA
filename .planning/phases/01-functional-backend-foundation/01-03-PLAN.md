---
phase: 01-functional-backend-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - dashboard/backend/adapters/review-store.ts
  - dashboard/backend/adapters/runtime-ops.ts
  - dashboard/backend/routes/review.ts
  - dashboard/backend/routes/runtime.ts
  - dashboard/backend/routes/index.ts
  - tests/dashboard-backend/routes.test.ts
  - tests/dashboard-backend/runtime-adapters.test.ts
autonomous: true
requirements:
  - ARCH-02
  - MIG-01
user_setup: []
must_haves:
  truths:
    - Typed backend routes exist for review and runtime resources and are backed by brownfield-safe adapters.
    - The backend contract layer is actually exercised through integrated route behavior.
    - The new backend can talk to the existing HYDRA runtime/data model without exposing legacy internals to clients.
  artifacts:
    - dashboard/backend/adapters/review-store.ts
    - dashboard/backend/adapters/runtime-ops.ts
    - dashboard/backend/routes/review.ts
    - dashboard/backend/routes/runtime.ts
    - tests/dashboard-backend/routes.test.ts
  key_links:
    - Route handlers validate payloads through contracts and normalize outputs through domain functions.
    - Runtime and review adapters isolate PM2/storage interactions from routes.
---

<objective>
Integrate the backend foundation into real review/runtime routes and prove the brownfield seam works.

Purpose: Turn the new backend skeleton and normalization layer into a usable contract boundary for later React work.
Output: Review/runtime adapters, typed routes, and integration coverage around backend behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-functional-backend-foundation/01-RESEARCH.md
@.planning/phases/01-functional-backend-foundation/01-01-PLAN.md
@.planning/phases/01-functional-backend-foundation/01-02-PLAN.md
@core/review-queue-db.js
@scripts/dashboard-review-routes.js
@scripts/dashboard-review-detail.js
@scripts/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build brownfield adapters for review storage and runtime operations</name>
  <files>dashboard/backend/adapters/review-store.ts, dashboard/backend/adapters/runtime-ops.ts</files>
  <action>Create narrow adapters that read review queue/transcript/classification/runtime status from the current HYDRA system and return data suitable for normalization. Keep PM2 and storage access out of route handlers. Be explicit about which legacy helpers are reused and which raw fields are intentionally hidden from clients.</action>
  <verify>npx vitest run tests/dashboard-backend/runtime-adapters.test.ts</verify>
  <done>Review and runtime side effects are isolated behind backend adapters.</done>
</task>

<task type="auto">
  <name>Task 2: Wire typed review and runtime routes through contracts and domains</name>
  <files>dashboard/backend/routes/review.ts, dashboard/backend/routes/runtime.ts, dashboard/backend/routes/index.ts</files>
  <action>Add route handlers that compose adapters + pure normalization + contract validation into stable backend resources for the future React app. Ensure route code remains thin and does not recreate business logic inline.</action>
  <verify>npx vitest run tests/dashboard-backend/routes.test.ts</verify>
  <done>Typed review/runtime routes exist and emit canonical operator-facing payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration coverage proving the brownfield backend seam works</name>
  <files>tests/dashboard-backend/routes.test.ts, tests/dashboard-backend/runtime-adapters.test.ts</files>
  <action>Add route/adaptor integration tests that prove the new backend consumes the current HYDRA runtime correctly and keeps legacy field names out of client payloads. Include at least one review flow and one runtime status/control flow.</action>
  <verify>npx vitest run tests/dashboard-backend/routes.test.ts tests/dashboard-backend/runtime-adapters.test.ts</verify>
  <done>Integration tests prove the backend seam works against real brownfield assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run tests/dashboard-backend/routes.test.ts tests/dashboard-backend/runtime-adapters.test.ts`
- [ ] `npx tsc --noEmit -p dashboard/tsconfig.json`
- [ ] Review/runtime routes expose canonical typed payloads backed by adapters
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The new backend seam is real, tested, and ready for the React shell in Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-functional-backend-foundation/01-03-SUMMARY.md`
</output>
