---
phase: 01-functional-backend-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/backend/contracts/review.ts
  - dashboard/backend/contracts/runtime.ts
  - dashboard/backend/domains/review-normalization.ts
  - dashboard/backend/domains/runtime-normalization.ts
  - tests/dashboard-backend/contracts.test.ts
  - tests/dashboard-backend/domains.test.ts
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
user_setup: []
must_haves:
  truths:
    - Dashboard review and runtime APIs are defined through typed contracts instead of raw route payloads.
    - Backend domain logic for normalization can be tested without PM2 or database side effects.
  artifacts:
    - dashboard/backend/contracts/review.ts
    - dashboard/backend/contracts/runtime.ts
    - dashboard/backend/domains/review-normalization.ts
    - dashboard/backend/domains/runtime-normalization.ts
    - tests/dashboard-backend/contracts.test.ts
  key_links:
    - Contract schemas parse and shape the outputs of normalization functions.
    - Domain functions are imported by tests without requiring route/server boot.
---

<objective>
Define the typed contracts and pure normalization logic for the new dashboard backend.

Purpose: Remove the naming drift that currently breaks reviewed-memory flows and establish a functional core for later backend routes.
Output: Typed DTO contracts, pure review/runtime normalization modules, and direct contract/domain tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-functional-backend-foundation/01-RESEARCH.md
@core/review-queue-db.js
@scripts/dashboard-review-routes.js
@scripts/dashboard-review-detail.js
@scripts/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed DTO contracts for review and runtime operator data</name>
  <files>dashboard/backend/contracts/review.ts, dashboard/backend/contracts/runtime.ts</files>
  <action>Create typed contract definitions for the new dashboard backend, using runtime validation so the frontend never consumes raw brownfield row shapes. Start with review queue/detail/action payloads and runtime status/control payloads. Encode canonical names such as `status`, `reviewNotes`, and normalized lifecycle values at this boundary.</action>
  <verify>npx vitest run tests/dashboard-backend/contracts.test.ts</verify>
  <done>Review and runtime payload schemas exist and establish canonical operator-facing field names.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure normalization functions for legacy review and runtime shapes</name>
  <files>dashboard/backend/domains/review-normalization.ts, dashboard/backend/domains/runtime-normalization.ts</files>
  <action>Implement functional domain modules that translate legacy dashboard/review/runtime data into canonical contract shapes. Keep these modules side-effect free so they can be tested directly. Avoid embedding DB access, PM2 execution, or HTTP concerns here.</action>
  <verify>npx vitest run tests/dashboard-backend/domains.test.ts</verify>
  <done>Pure normalization functions exist for reviewed-memory and runtime operator payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract and domain tests that lock the canonical vocabulary</name>
  <files>tests/dashboard-backend/contracts.test.ts, tests/dashboard-backend/domains.test.ts</files>
  <action>Add tests that prove legacy field names map into canonical contract names and that invalid/partial payloads are rejected or normalized intentionally. Use real examples from the current review queue/dashboard routes so the tests prevent future naming drift.</action>
  <verify>npx vitest run tests/dashboard-backend/contracts.test.ts tests/dashboard-backend/domains.test.ts</verify>
  <done>Contract and domain tests protect the new backend vocabulary against regression.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run tests/dashboard-backend/contracts.test.ts tests/dashboard-backend/domains.test.ts`
- [ ] `npx tsc --noEmit -p dashboard/tsconfig.json`
- [ ] Review/runtime canonical field names are defined in one place
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Contract normalization prevents legacy naming drift from leaking into the new backend
</success_criteria>

<output>
After completion, create `.planning/phases/01-functional-backend-foundation/01-02-SUMMARY.md`
</output>
