# ══════════════════════════════════════════════════════════════════
# HYDRA — sample.env
# Copy to .env and fill in all required values before starting.
# ══════════════════════════════════════════════════════════════════

# ── Core LLM ──────────────────────────────────────────────────────
# OpenRouter API key for all agent LLM calls
OPENROUTER_API_KEY=

# ── Slack ─────────────────────────────────────────────────────────
# Slack Bot User OAuth Token (xoxb-...)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack Signing Secret (from App settings → Basic Information)
SLACK_SIGNING_SECRET=your-signing-secret

# Slack App-Level Token for Socket Mode (xapp-...)
SLACK_APP_TOKEN=xapp-your-app-token

# Default channel for budget/circuit-breaker alerts (optional)
SLACK_STATUS_CHANNEL=#hydra-status

# ── Storage ───────────────────────────────────────────────────────
# Path to the brain directory on Mac Mini internal storage
# This holds SQLite, LanceDB, agent state, daily logs, usage data
BRAIN_PATH=~/hydra-brain

# Path to external SSD for heavy/bulk data
# Audio files, screenpipe raw captures, backups staging, media
EXTERNAL_SSD_PATH=/Volumes/HydraSSD

# ── Home Assistant (05-jarvis) ────────────────────────────────────
HOME_ASSISTANT_URL=http://192.168.68.124:8123
HOME_ASSISTANT_TOKEN=

# HA entity IDs (override if your HA entity IDs differ)
HA_AC_ENTITY=climate.living_room_ac
HA_BEDROOM_LIGHTS=light.bedroom
HA_DESK_LAMP=light.desk_lamp
HA_MOTION_SENSOR=binary_sensor.aqara_motion_p1
HA_TEMP_SENSOR=sensor.tapo_t310_temperature
HA_DOOR_SENSOR=binary_sensor.door_sensor

# ── CFO-Bot (06-cfobot) ───────────────────────────────────────────
# Your monthly take-home income (₹, used for wedding fund calculation)
CFO_MONTHLY_INCOME=0

# Planned monthly debt repayment amount (₹)
CFO_MONTHLY_DEBT_PAYMENT=0

# ── Wolf Paper Trading (09-wolf) ──────────────────────────────────
# Perplexity API key for real-time market research
PERPLEXITY_API_KEY=

# ── BrandBot GitHub (02-brandbot) ────────────────────────────────
GITHUB_USERNAME=
GITHUB_TOKEN=

# ── Mercenary Invoicing (10-mercenary) ───────────────────────────
# Bank account details to include on invoices (plain text, e.g. "HDFC xxxx1234")
BANK_DETAILS=
# Your UPI ID for payments
UPI_ID=

# ── Health Sync (scripts/health-sync.js) ──────────────────────────
# Override CSV download source dir (default: ~/Downloads)
HEALTH_CSV_SOURCE=

# Override iCloud Health Auto Export folder (default: ~/Library/Mobile Documents/...)
HEALTH_ICLOUD_DIR=

# Override health data output dir (default: {BRAIN_PATH}/brain/07_BIOBOT/health_data)
HEALTH_DATA_DIR=

# ── Screenpipe Sync (scripts/screenpipe-sync.js) ──────────────────
# Override Screenpipe SQLite path (default: ~/Library/Application Support/screenpipe/db.sqlite)
SCREENPIPE_DB=

# Override screen context output dir (default: {BRAIN_PATH}/brain/01_EDMO/screen_context)
SCREENPIPE_CTX_DIR=

# ── OpenClaw Gateway (core/openclaw.js) ───────────────────────────
# Path to the openclaw CLI binary (default: openclaw, must be in PATH)
# Install: npm install -g openclaw@latest
OPENCLAW_BIN=openclaw

# Timeout in ms for OpenClaw CLI calls (default: 15000)
OPENCLAW_TIMEOUT=15000

# Port for SocialBot webhook receiver (default: 3004)
# OpenClaw forwards incoming messages here via hooks.onMessage
SOCIAL_WEBHOOK_PORT=3004

# ── Internal Service Auth ─────────────────────────────────────────
# API key for internal service-to-service calls
INTERNAL_API_KEY=

# ── Health Endpoint ───────────────────────────────────────────────
# Port for per-agent /health endpoint (default: 3002)
HEALTH_PORT=3002

# ── Timezone ─────────────────────────────────────────────────────
# IANA timezone string (default: Asia/Kolkata)
TZ=Asia/Kolkata

# ── Backblaze B2 Backup (scripts/backup.sh) ──────────────────────
B2_ACCOUNT_ID=
B2_APP_KEY=
B2_BUCKET=hydra-backup
RCLONE_CRYPT_PASSWORD=
